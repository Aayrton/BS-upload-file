{% extends "BSUserBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}

{% block body %}
    <div>
        <a class="btn btn-primary btn-lg"  href="{{ path('fos_user_security_login') }}">Login</a>
        <a class="btn btn-primary btn-lg"  href="{{ path('upload') }}">Upload</a>
        <a class="btn btn-primary btn-lg"  href="{{ path('fos_user_security_logout') }}">Logout</a>
    </div>
{% endblock %}